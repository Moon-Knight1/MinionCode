@startmindmap
+ MinionCode 子Agent架构 (SubAgent)

++ 一、 痛点与核心同构洞察
+++ 核心痛点：上下文污染与终端噪声
++++ 单个 Agent 处理复合任务时，历史读取的文件、diff、报错等非相关信息会堆积在同个对话窗口中
++++ 导致模型推理注意力被严重稀释，Token 消耗呈指数暴涨，响应质量发生断崖式下跌
+++ 关键洞察：Agent 也是一种工具
++++ 抽象同构：Agent 接收任务（参数）并返回报告（结果），在接口特征上与原子 Tool 完全同构
++++ 架构收拢：在工具层注册一个统一的 Agent 工具（AgentTool），通过 subagent_type 参数动态路由类型
++++ 收益：对外暴露稳定的工具列表与 Schema 约束，对主大模型而言，委派子 Agent 与调用 Bash 一样自然

++ 二、 定义式 vs Fork式（两种创建模式）
+++ 1. 定义式（Definition-based）：预定义的只读专家
++++ 机制：基于独立的 Markdown 配置文件加载（YAML frontmatter 定义元数据 + Markdown body 写入系统提示）
++++ 特性：启动时建立完全空白的对话上下文，通过 disallowedTools 黑名单严格锁死能力边界
++++ 价值：专人专事（如安全审查、代码探索），由于工具集受限且无写副作用，可结合 dontAsk 权限模式实现全自动前台/后台运行
+++ 2. Fork式（Fork-based）：继承上下文的临时执行工人
++++ 机制：当调用 Agent 工具且 subagent_type 留空时触发
++++ 特性：克隆并继承父 Agent 的完整对话历史，但在末尾强制注入一段 <fork_boilerplate> 强硬指令
++++ 约束：强行覆盖大模型爱和用户拉扯、提问的倾向，强迫其切换为只做不说的执行状态
++++ 缓存优化：利用与父 Agent 完全相同的消息前缀，实现 100% 触发 API 的 Prompt Cache（提示词缓存）
++++ 运行语义：无条件在后台异步运行，主进程立即拿回控制权，避免长上下文首轮生成造成前台挂起卡死

++ 三、 上下文隔离与共享边界
+++ 强隔离运行时状态 (防止数据交织)
++++ 隔离消息历史：定义式从零开始确保零噪声；Fork 式共享历史但独立后续演进
++++ 隔离追踪状态：独立创建权限追踪器（PermissionTracker）与文件状态缓存（FileStateCache）
+++ 强共享基础设施 (节约建立开销)
++++ 共享底层 LLM 客户端与底层网络连接池
++++ 共享全局 Hook 引擎（无论是谁写文件，都能自动触发 Prettier 格式化或 Lint 检查）
++++ 共享同个项目文件系统空间（除非在调用参数中显式开启了 isolation="worktree"）

-- 四、 任务级非交互式运行模式 (RunToCompletion)
--- 机制本质
++++ 子 Agent 采用非交互式 Loop，任务直接由参数传入，不需要人在控制台前进行实时输入
--- 停止条件
++++ 当大模型在某一轮 ReAct 迭代后，不再输出任何 tool_call 块、转为纯文本报告时，循环自动安全断开
--- 全自动协同
++++ 结合 disallowedTools 黑名单排除写工具后，直接把权限降级为 dontAsk 自动化放行，达成全自理闭环

-- 五、 嵌套调用防御与工具过滤四防线
--- 嵌套深度防御 (防止成本与长链失控)
++++ 限制 1：Fork 路径子 Agent 严禁二次派生 Fork（基于 Fork 标记扫描强行阻断）
++++ 限制 2：后台异步运行的子 Agent 被切断再次 spawn 子 Agent 的权限（异步白名单中不含 Agent 工具）
++++ 限制 3：全局禁止列表中剔除 Agent 工具本身，在定义层实现隐式递归阻断
--- 四层工具过滤流水线
++++ 第 1 层 (全局层)：ALL_AGENT_DISALLOWED_TOOLS 过滤子 Agent 均不可见的系统敏感工具
++++ 第 2 层 (自定义层)：CUSTOM_AGENT_DISALLOWED_TOOLS 拦截特定的第三方扩展漏洞工具
++++ 第 3 层 (后台层)：ASYNC_AGENT_ALLOWED_TOOLS 当子 Agent 在后台跑时，工具集直接硬降级收窄至基础读写集
++++ 第 4 层 (定义层)：依据配置文件的 tools 白名单和 disallowedTools 黑名单执行最终交集演算

-- 六、 内置 Agent 专家团卡片
--- Explore (代码探索专家)
++++ 属性：基于 haiku 小模型运行，排除写工具。善用 Glob、Grep、Read 专门在仓库中扒梳结构和调用链
--- Plan (计划架构师)
++++ 属性：排除写工具和子 Agent。在独立上下文里深度推演方案，在返回时只给主 Agent 吐出最终实现步骤
++++ 对比：与主 Agent 本身的 Plan 权限模式不同，此工具利用独立上下文，不向主历史灌入任何中间探索垃圾 Token
--- General-purpose (通用执行者)
++++ 属性：拥有全套原子工具集，专门用来承接主 Agent 拆分出来的、需要全能力支撑的子技术模块实现
--- Verification (自动测试门禁)
++++ 属性：通过配置文件开关按需开启。以强烈的怀疑视角运行，严防“验证回避”与“被前 80% 漂亮 UI 迷惑”的幻觉，专门用构建、跑测试、查类型等硬性命令去打破实现，寻找最后 20% 的隐藏 Bug

-- 七、 后台管理工具集 (Task API)
--- 任务进入后台的三大路径
++++ 路径 1：主模型在前台调用工具时，显式传参 run_in_background: true
++++ 路径 2：前台运行的同步子 Agent 超过 120 秒未产生终态，被 getAutoBackgroundMs() 超时策略强行 adoptRunning 移交至后台
++++ 路径 3：人类用户在终端按下 ESC 键，手工将当前耗时任务切入后台挂起
--- 自然语言工具接口
++++ 系统不为人类提供任务管理斜杠命令，转而为大模型提供 TaskList、TaskGet、TaskCreate、TaskUpdate 四个内置业务工具
++++ 主模型利用这些工具查询后台进度并用自然语言转述给人类，结果通过特定的 <task-notification> 标签异步注入主历史

@endmindmap