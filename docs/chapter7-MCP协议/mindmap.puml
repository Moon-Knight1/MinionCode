@startmindmap
+ MinionCode MCP工具生态

++ 一、 MCP 概念与解耦价值
+++ 核心痛点
++++ 工具供给与 Agent 核心硬编码耦合，新增工具需修改代码并重新编译发布
++++ 无法应对全世界第三方生态 API 的海量集成需求
+++ USB 标准化类比
++++ 传统对接：M 个 Agent 对接 N 个工具需要 M 乘以 N 个专用代码适配器
++++ MCP 对接：降低为 M 加 N 个标准化对接，Agent 仅实现一次客户端，工具仅实现一次服务端
+++ 核心价值
++++ 实现工具的动态接入与供给解耦，MinionCode 无需改动任何核心代码即可直接调用外部生态

++ 二、 运行时角色与协议分层
+++ 运行时三大角色
++++ Host：AI 应用本体（如 MinionCode），充当顶层管理者
++++ Client：Host 内部创建的连接组件，每个 Client 独立对接一个特定的 Server 并管理其生命周期
++++ Server：对外暴露原子能力的独立程序，可运行在本地或远程
+++ 协议双层架构
++++ 数据层 (Data Layer)：定义消息语义、初始化握手、能力协商（工具/资源/提示词的列举与调用）
++++ 传输层 (Transport Layer)：定义底层的通道发送方式（如管道、HTTP），与上层数据语义解耦

++ 三、 Server 三种核心原语
+++ Tools (核心)
++++ 外部进程中执行的原子能力，向 Client 提供名称、描述与参数 JSON Schema，完美映射内部工具接口
+++ Resources (数据源)
++++ 供 Agent 读取的静态或动态上下文背景数据（如数据库表结构、长文本日志），通过特定 URI 请求
+++ Prompts (提示词模板)
++++ 外部服务商预定义的提示词模板，Agent 传入特定参数后返回组装好的文本，用于引导特定生成
+++ Client 侧对等能力
++++ 包含 Roots（宣告项目根目录边界）、Sampling（允许 Server 反向请求 Host 调用 LLM）等能力

++ 四、 传输层标准：stdio 与 Streamable HTTP
+++ stdio 传输 (本地子进程)
++++ 原理：Host 将 Server 作为子进程启动，通过标准输入输出（stdin/stdout）管道进行密闭通信
++++ 优势：无网络开销、无需暴露端口、免除服务发现，随 Host 进程启动而建立、退出而清理
++++ 边界：Server 的 stdout 必须严格保持纯净的协议 JSON 串，任何日志与调试信息必须重定向至 stderr
+++ Streamable HTTP 传输 (远程独立服务)
++++ 原理：Server 作为独立的常规 HTTP 服务运行，Client 通过标准的 HTTP POST/GET 发送 JSON-RPC 请求
++++ 响应：就绪结果返回 application/json，长耗时或流式推送返回 text/event-stream (SSE)
++++ 工程特性：需要额外处理网络超时、重试机制、TLS 证书以及自定义 Header 认证（如 API Key 校验）

-- 五、 JSON-RPC 2.0 消息格式与异步匹配
--- 三种基本消息类型
++++ 请求 (Request)：包含唯一 id、method、params，客户端发出后处于阻塞或等待响应状态
++++ 响应 (Response)：包含与请求对应的 id、result 或 error 错误域，由服务端处理后回传
++++ 通知 (Notification)：包含 method、params，但不包含 id，发出即结束，不需要任何接收方回应
--- 请求响应异步匹配机制
++++ 痛点：单管道内连续发送多条请求时，服务端的响应可能会发生交错（如先回 id 2 再回 id 1）
++++ 实现：客户端构建等待字典（Pending Map），发送请求时生成自增 id 并注册 response 通道，读取循环通过 id 精确分发

-- 六、 工具包装器与配置合并
--- 工具包装器 (MCPToolWrapper)
++++ 运用适配器模式，将 MCP 外部工具包装、隐藏为内部通用的 Tool 接口
++++ 命名规约：加前缀命名为 mcp_serverName_toolName，彻底消除多 Server 间同名工具的命名冲突
--- 多作用域配置合并
++++ 项目级配置：项目根目录配置文件，针对项目深度定制，支持变量注入（如 `${GITHUB_TOKEN}`）
++++ 用户级配置：家目录全局配置文件，跨项目共享通用工具（如 Slack）
++++ 合并策略：项目级覆盖用户级同名 Server，最终合集对顶层 Agent 表现完全透明

-- 七、 启动流水线与工具延迟加载
--- 完整启动与调用流水线
++++ 读取配置 -> 创建 Transport -> 启动时主动建立后台连接能力协商 -> 初始化握手 -> 工具拉取包装注册 -> Agent 动态路由调用
++++ 避坑：不可采用懒加载（用到才连），否则模型在首轮无法感知工具存在，必须在应用启动时后台异步批量完成初始化
--- 工具延迟加载机制 (ToolSearch)
++++ 痛点：MCP 引入导致工具数暴增（如 80 个以上），导致全量加载耗费数万 Token，且严重稀释模型注意力
++++ 机制：默认仅在 System Reminder 中列出延迟加载工具的名称，不塞入完整参数 Schema
++++ 触发：模型判断需要该工具时，调用 ToolSearch 工具进行精确或关键词检索，命中后再按需加载其完整 Schema
++++ 优势：纯客户端自主实现延迟管理，完美兼容所有主流 LLM 厂商，摆脱对特定厂商专有字段的强依赖

-- 八、 安全考量与信任边界
--- 权限系统联动
++++ 所有经由 MCP 转换而来的外部工具同样严格受制于内部的五层纵深权限控制体系
--- 细粒度规则匹配
++++ 允许在权限配置文件中针对 MCP 工具定制专属规则，如 mcp_github_* 予以放行，而 mcp_*_delete_* 予以 Deny
--- 异常反馈循环
++++ MCP 工具执行失败或权限拦截产生 isError 为 true 的规范结果，发回模型使其在下一轮自发调整策略重试

@endmindmap