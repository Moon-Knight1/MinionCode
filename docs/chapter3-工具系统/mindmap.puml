@startmindmap
+ MinionCode 工具系统

++ 一、 Function Calling 协议机制
+++ 核心本质：决策与执行分离
++++ 模型仅负责思考和决策生成文本
++++ 客户端代码负责在本地执行具体操作
++++ 执行结果反馈回模型，驱动下一步思考
+++ 四步时序流程
++++ 1. 宣告：通过 tools 参数向模型声明可用工具的名称、描述与参数 Schema
++++ 2. 请求：模型输出 role 为 assistant 且 type 为 tool_use 的结构化 JSON，包含唯一 id
++++ 3. 执行：客户端拦截请求并在本地运行，将结果作为 user 角色的 tool_result 发回
++++ 4. 推进：模型根据工具返回结果，决定给出最终回复或触发下一轮工具调用

++ 二、 工具描述与接口设计
+++ 工具描述的决定性作用
++++ 描述质量直接决定模型的工具使用行为
++++ 规范要素：做什么、何时该用、何时不该用、参数约束、返回格式、工作流建议
+++ 生产级工具接口要素
++++ 元信息：name(), description(), inputSchema(), category()
++++ 权限控制：isReadOnly()（只读标志）、isDestructive()（破坏性标志，如 Bash）
++++ 并发控制：isConcurrencySafe()（标记是否允许与其他工具并发执行，只读工具默认为 true）
++++ 输入校验：validateInput()（执行前校验参数，失败则返回错误结果供模型调整重试）
+++ 执行结果设计 (ToolResult)
++++ content：返回给模型的文本内容
++++ isError：标记是否为工具执行失败（文件不存在、权限不足等属于有价值反馈，系统崩溃才抛出异常）
++++ metadata：给 UI 用的额外元数据（如耗时），不发给模型以节省 Token

++ 三、 工具注册中心与客户端集成
+++ 工具注册中心 (Registry)
++++ 统一管理入口，实现工具创建与使用的解耦
++++ 支持条件启用（如根据配置项判断是否开启写操作或 Bash 授权）
++++ 提供 toAPIFormat() 集中组装工具定义参数
+++ 流式 tool_use 解析核心逻辑
++++ 文本流直接追加，而工具参数流入的是 JSON 碎片 (input_json_delta)
++++ 1. 拦截 content_block_start：若 type 为 tool_use，记录 id/name 并初始化字符串缓冲区
++++ 2. 累加 content_block_delta：将持续到达的 partial_json 碎片追加至缓冲区
++++ 3. 触发 content_block_stop：将缓冲区完整字符串解析为 JSON，触发 ToolUse 事件

-- 四、 六大核心工具设计考量
--- ReadFile (file 分类)
++++ 属性：只读，非破坏性，并发安全
++++ 细节：返回文本必须带行号前缀；支持 offset/limit 分段读取大文件；前 512 字节含 NUL 则判定二进制并拒绝
--- WriteFile (file 分类)
++++ 属性：非只读，非破坏性，串行执行
++++ 细节：完整覆盖写入；执行前需递归创建不存在的父目录
--- EditFile (file 分类)
++++ 属性：非只读，非破坏性，串行执行
++++ 细节：基于 old_string 和 new_string 精确替换；old_string 必须在文件中唯一匹配，不唯一或未找到则报错
--- Bash (shell 分类)
++++ 属性：非只读，破坏性，串行执行
++++ 细节：暴露操作系统能力；设置 30 秒强超时防止卡死；合并 stdout 和 stderr 并在超限时截断保留首尾
++++ 避坑：非零退出码（如编译报错）代表正常反馈信息，非 isError，只有超时才算 error
--- Glob (search 分类)
++++ 属性：只读，非破坏性，并发安全
++++ 细节：按通配符模式找文件名；自动排除 git 和 node_modules 等干扰目录；按修改时间倒序限制最多 200 条
--- Grep (search 分类)
++++ 属性：只读，非破坏性，并发安全
++++ 细节：按正则表达式搜文件内容；输出格式为路径加行号加内容；自动过滤二进制，限制最多 100 条

-- 五、 消息管道与转换适配
--- 管道模式演进
++++ 引入工具后，消息链条变为 user -> assistant(含 tool_use) -> user(含 tool_result) -> assistant
--- 转换适配约束
++++ 约束 1：tool_result 必须作为 user 角色发送
++++ 约束 2：单次回复中的 text 块与 tool_use 块必须留存在同一条 assistant 消息中
++++ 约束 3：并行调用的多个 tool_use 与多个 tool_result 必须分别在同条消息内通过 id 配对

@endmindmap