@startmindmap
+ MinionCode Hook系统

++ 一、 Hook系统本质与三要素
+++ 核心本质：事件驱动的自动化引擎
++++ 针对生命周期内的特定关键节点，绑定确定的自动化动作，规避人肉操作
+++ Hook三大核心要素
++++ 事件 (When)：触发自动化的特定运行时机节点
++++ 条件 (If)：决定Hook是否在触发后真正执行（不写条件代表无条件执行）
++++ 动作 (Do)：满足条件时在本地或外部执行的特定具体操作
+++ 多层作用域配置合并
++++ 包含三层：本地临时层、项目配置层、用户全局层
++++ 语义差异：采用追加合并（Append）策略使各层Hook同时叠加生效，与技能系统的硬性覆盖不同

++ 二、 生命周期事件矩阵
+++ 会话级事件
++++ session_start（新会话开启）、session_end（会话完全闭合），单次生命周期仅触发一次
+++ 轮次级事件
++++ turn_start（接收到用户新消息）、turn_end（模型完全结束回复）
+++ 工具级事件 (最高频场景)
++++ pre_tool_use（工具执行前拦截/前置处理）、post_tool_use（工具执行后追溯/后置处理）
+++ 消息级事件
++++ pre_send（消息递交给大模型前修改/注入）、post_receive（接收到模型响应后的数据清洗）
+++ 系统级事件
++++ startup、shutdown、error、compact（上下文压缩时）、permission_request（权限审批时）、file_change（文件变更）

++ 三、 核心拦截点 pre_tool_use
+++ 差异机制：属于系统唯一的非通知型、可说不的同步拦截事件
+++ 拦截熔断 (reject)
++++ frontmatter 声明 reject 为 true 时，可根据工具参数的内容实施细粒度硬阻断
++++ 命中规则后阻断后续关联Hook链，终止底层工具运行并向引擎向上抛出 ToolRejectedError
+++ 反馈闭环
++++ 阻断后的失败提示文本原样组装为工具执行结果，作为 user 角色发回给模型循环
++++ 驱动大模型在下一轮感知“该途径不被允许”，使其自主变换其他动作策略重新尝试

++ 四、 条件语法与解析约束
+++ 核心匹配对象：从 HookContext 提取核心字段（tool工具名、event事件名、args.xxx参数特定字段）
+++ 比较操作符矩阵
++++ ==（精确值相等匹配）与 !=（反向不等匹配）
++++ =~（高效正则匹配，用于复杂文本、空格变体的强验算）
++++ ~=（通配符 glob 路径匹配，*匹配单层路径，**匹配多层级路径，?匹配单个字符）
+++ 逻辑组合约束
++++ 支持使用双和与双或进行条件链条组合，但禁止在一行内进行 && 和 || 的混合交叉书写
++++ 目的：保持配置的可控性，免除运算符优先级带来的复杂语义解析成本

-- 五、 四大动作执行器
--- 1. command (外部命令执行器)
++++ 场景：最高频动作（如写代码文件后触发自动跑格式化、lint校验或 proto 编译代码生成）
++++ 机制：派生外部 shell 子进程独立运行，支持配置超时字段（timeout）进行超时强杀防卡死
--- 2. prompt (提示词注入器)
++++ 场景：多用于会话初始或每轮开始前为模型动态喂入新的项目规则或阶段性架构设计
++++ 机制：使用特定的标签将其作为系统 reminder 块塞入 system 区域，绝不污染消息历史结构
--- 3. http (远程Webhook通知器)
++++ 场景：执行特定关键动作后异步发送外部网络请求（如向 Slack 团队频道推送 Agent 修改审计日志）
--- 4. agent (子Agent分派器)
++++ 场景：开启一个全新的子大模型实例来审查当前事件（如 AI 审计 AI），属于高阶能力编排
++++ 机制：接口层在前，具体运行时依赖 SubAgent 运行时的回调和生命周期管理器接管

-- 六、 执行控制机制与鲁棒性
--- 触发频次控制 (once)
++++ 声明 once 为 true 时，当前 Hook 实例在本次进程会话周期内首次执行后即打上已执行标记
++++ 后续轮次天然跳过，防止长对话或循环中出现重复注入，程序重启后重置该执行标记
--- 后台异步隔离 (async)
++++ 耗时长的外发型动作支持配置 async 为 true，直接抛给后台协程/线程池，完全不阻塞主模型的推理
++++ 强约束限制：pre_tool_use 拦截事件由于需要同步承接阻断决策，绝对禁止声明为异步模式
--- 尾巴摇狗防御 (鲁棒性基线)
++++ 架构铁律：Hook 仅作为核心循环的辅助插件扩展，其动作在发生程序级异常或崩溃时只记录 error 日志
++++ 绝对不向核心引擎层扩散异常，防止辅助自动化故障反向拖垮正常的 Agent 主任务

-- 基本流程替换：提取模板字符 -> 正则或字符串置换 -> 解析未知符号为空字符串
-- 与 Agent 循环深度集成
--- 拦截触发点：精准插在 executeTool 真实运行的前后两侧
--- 拦截数据流：同步塞入上下文，检查合法性约束，不合规直接回传降级结果

@endmindmap