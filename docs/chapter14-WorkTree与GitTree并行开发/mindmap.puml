@startmindmap
+ MinionCode 文件系统隔离

++ 一、 核心痛点与 Git Worktree 引入
+++ 冲突灾难
++++ 后台并行子 Agent 或团队队员同时修改同个文件（如 server.py），会引发读写错乱和文件覆盖
+++ 为什么标准 Git 分支不够
++++ 分支提供的是时间维度的快照隔离，但无法做到空间维度的多目录并行
++++ 只有一个工作目录，切换分支会频繁重置文件时间戳（mtime），破坏增量构建导致大面积重编
+++ Git Worktree 机制 (Git 2.5+)
++++ 核心定位：共享同一个 Git 仓库版本历史，物理隔离出多个独立的工作目录
++++ 优势：Agent 之间同时在不同的目录中改代码、跑编译、测用例，互不干扰且版本历史天然统一

++ 二、 Worktree 管理器与会话状态
+++ 状态维护 (WorktreeManager)
++++ 持有主仓库路径（repoRoot）、工作树总根目录（worktreeDir）、并发保护互斥锁（lock）及活跃工作树映射（active）
++++ 内部直接持有文件缓存（fileCache）引用，将缓存清理与工作树生命周期强绑定，防止上层漏删
+++ 会话持久化 (WorktreeSession)
++++ 记录进入前的原工作目录（originalCwd）、原 HEAD 提交、Worktree 路径及分支名
++++ 状态持久化至本地的 worktree_session.json 文件中，作为客户端进程崩溃后 --resume 快速恢复的底层依据

++ 三、 Slug 安全校验与创建优化
+++ Slug 路径遍历防御
++++ 校验规则：限制总长 64 字符，仅允许字母、数字、点号、连字符、下划线
++++ 强行拦截：虽然允许点号，但必须显式拒绝纯 . 或 .. 的独立段命名，斩断 LLM 幻觉诱发的路径遍历攻击
++++ 命名映射：允许使用 / 表达复合层级（如团队名/队员名），在转换成分支名时将 / 替换为 +，规避 Git 目录/文件冲突
+++ 快速恢复优化 (Fast Recovery)
++++ 机制：当发现 Worktree 目录已存在时，直接通过纯文件系统读取其 .git 指针文件和 HEAD 引用
++++ 性能：整个恢复过程仅需约 3ms
++++ 价值：在大仓库中彻底免去了调用 git fetch 扫描本地 commit-graph 耗时 6-8 秒的沉重开销

++ 四、 创建后的四项初始化动作 (Best-effort)
+++ A. 本地配置复制
++++ 自动将主仓库中不入库的 settings.local.json、密钥及环境变量文件拷贝到新工作区
+++ B. Git Hooks 继承
++++ 显式把主仓库的 core.hooksPath（如 .husky）重新配置到 Worktree 的本地 git config 中，确保代码提交校验不失效
+++ C. 软链接大依赖目录
++++ 读取配置，将 node_modules、.venv、vendor 等动辄数百 MB 的依赖目录以软链接形式挂载，节省磁盘
++++ 避坑：部分语言运行期解析 symlink 会直达真实路径（如 Node.js 的 __dirname 会飘回主仓库），必要时需开启保护参数
+++ D. 被忽略文件补偿
++++ 依据项目专属的 .worktreeinclude 配置文件，用 git ls-files 过滤出未入库但运行时强依赖的文件（如 .env）复制过去

-- 五、 进入与退出的状态控制边界
--- 显式工作目录机制 (Explicit Cwd)
++++ 拒绝使用全局进程级的 chdir 切换工作目录，防止多线程/多协程并发组件发生时序竞争碰撞
++++ 机制：将目标工作树路径记录在当前会话中，所有内置原子工具（Bash、Read、Write）每次执行时显式提取该路径作为本地 cwd
++++ 衍生收益：各工作树路径在文件系统上天然绝对唯一，内存文件缓存 key 不会发生任何碰撞，无需执行高昂的清缓存动作
--- 退出变更保护
++++ 拦截校验：当执行退出且动作声明为 remove（删除）时，调用 git status 与 rev-list 盘点改动
++++ 拦截条件：若工作树内存在未提交的修改或未合并的新 commit，直接拒绝删除，必须显式声明 discardChanges=true 才能强杀
--- 状态重置
++++ 退出时强制执行 chdir 回原工作目录 originalCwd 进行兜底，同时将 worktree_session.json 的持久化内容清空为 null

-- 六、 变更保护与多级自动清理
--- 自动化清理策略 (autoCleanup)
++++ 规则：子 Agent 正常结束任务退出时，系统自动核对工作树是否有代码沉淀
++++ 分支 1：若子 Agent 只是读取、分析文件而无任何修改，直接调用 git worktree remove 强拆删除
++++ 分支 2：若子 Agent 写了源码或产生了新的提交，则强制保留工作树，并将路径和分支名追加返回，提示主 Agent 审核
--- 后台过期清理安全网
++++ 定期扫描：后台异步例行扫描 .mewcode/worktrees/ 目录
++++ 临时识别：通过正则表达式仅匹配特定特征的临时工作树（如子 Agent 创建的 agent-a[0-9a-f]{7}），绝对不碰人类用户手动命名的分支工作区
++++ Fail-closed 三层过滤：即使临时工作树过期，只要触发“有未提交修改”或“存在未推送到远端（unpushed）的 commit”中任意一条，直接跳过不删，宁可多耗磁盘也绝不丢失任何代码资产

-- 七、 隔离环境与 SubAgent 天然集成
--- 编排闭包
++++ 通过 Agent 定义配置文件中的 isolation: worktree 字段，将上下文隔离与文件系统隔离融为一体
--- 运行管线串联
++++ 主 Agent 触发委托 -> 自动生成 8 位随机 Hex 临时工作树 -> 注入 Worktree 上下文通知（告知子 Agent 其处于副本中，需翻译路径并重新读取） -> 创建子实例并注入专属 cwd -> 子 Agent 独立非交互式运行 -> 产出 commit -> 回传状态并触发自动清理
--- 架构留白
++++ 系统未内置 merge 业务工具，子 Agent 产出提交后，交由主 Agent 在主历史中利用 Bash 自行跑 git merge 或 cherry-pick，从而保证团队合并冲突和恢复路径完全暴露在主模型的视野与上下文控制中

@endmindmap