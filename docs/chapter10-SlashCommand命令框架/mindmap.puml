@startmindmap
+ MinionCode 命令系统

++ 一、 痛点与本地快车道机制
+++ 传统对话局限
++++ 清屏、查询Token、切换模式等UI层操作不需要AI参与
++++ 走大模型循环会白白消耗Token且响应慢
+++ 本地快车道 (Slash Command)
++++ 以斜杠开头的输入被解析器强行拦截
++++ 绕过Agent Loop直接在本地处理，响应速度达毫秒级
++++ 行为完全确定，不受模型随机性干扰

++ 二、 命令框架核心三要素
+++ 声明式注册中心
++++ 集中管理命令的身份证信息（名称、别名、描述、用法、类型、隐藏属性）
++++ 内部使用读写锁保护，保证未来运行时动态扩展命令时的并发安全
+++ 解析器分流
++++ 剥离斜杠并按第一个空格将输入拆分为命令名与参数
++++ 命令名强转小写以消除大小写不敏感带来的匹配问题
+++ 统一上下文背包 (CommandContext)
++++ 打包参数、Agent实例、对话、会话、配置及UI控制器依赖
++++ 内部解耦：通过UIController接口处理系统消息或发送模拟消息，命令无需关心UI渲染细节

++ 三、 三大命令执行类型
+++ 本地纯文本命令 (local)
++++ 纯本地运行，不改变系统与UI状态，直接以系统消息形式反馈结果
++++ 典型命令：help, status, session list
+++ 影响UI状态命令 (local-ui)
++++ 本地执行的同时会改变UI组件的行为模式、状态栏或者重置当前界面
++++ 典型命令：clear, plan, do
+++ 转发AI快捷命令 (prompt)
++++ 本地不执行具体业务，仅充当快捷方式，将精心设计的Prompt模板发送给Agent
++++ 走正常Agent Loop并消耗Token，省去用户记忆复杂Prompt的成本
++++ 典型命令：review

-- 四、 交互与体验优化
--- 快捷别名 (Aliases)
++++ 提供高频操作的极简缩写（如 /c 对应 /compact）
++++ 注册期执行强冲突检测，若别名碰撞则直接触发Panic以确保行为可预测
--- 参数缺失提示 (argPrompt)
++++ 当必填参数缺失时，动态抛出友好的具体用法引导提示而非冷冰冰报错
--- Tab智能补全 (Autocomplete)
++++ 自动检索非隐藏命令的名称与别名
++++ 单个匹配直接上屏填充，多个匹配列出下拉菜单供用户选择

-- 五、 UI事件循环集成边界
--- 分流拦截时机
++++ 强行插在输入框提交后、发给Agent Loop之前进行硬判断
--- 空输入保护
++++ 过滤纯空格与空输入，防止误触回车白白触发API调用
--- 异常状态降级
++++ 未知命令统一输出友好提示，并强引导至 /help 入口
++++ 状态栏根据当前激活模式实时展示动态的可用快捷命令提示

-- 六、 十大核心内置命令清单
--- 纯本地命令
++++ /help：显示命令列表或特定命令的详细用法引导
++++ /compact：手动强制压缩上下文并直观呈现前后Token对比
++++ /session：提供新建、切换、列出与删除历史会话的完备管理
++++ /memory：审计和手动管理长期记忆，提供清理与强行常驻功能
++++ /permission：实时切换权限模式并审计当前生效的硬拦截规则
++++ /status：一键展示模式、Token占比、工具数、记忆数等综合看板
--- UI状态命令
++++ /clear：安全归档当前历史并重置界面开启全新干净会话
++++ /plan：一键切入规划模式，支持直接带参数作为任务描述发起探索
++++ /do：切回标准执行模式，驱动模型按既定计划动手干活
--- 转发快捷命令
++++ /review：自动化读取当前代码仓的git diff并触发深度的代码审查

@endmindmap