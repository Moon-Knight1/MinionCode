@startmindmap
caption MinionCode 驾驶手册与 Prompt 管线脑图
title MinionCode 驾驶手册：System Prompt 体系与组装管线

+ MinionCode 驾驶手册

++ 一、 System Prompt 七大核心模块
+++ 1. 角色设定
++++ 明确身份为终端环境中的 AI 编程助手
++++ 锁定四种核心任务：修 bug、添加功能、重构代码、解释代码
++++ 作用：收窄模型的行为空间，让决策更具可预测性
+++ 2. 行为准则
++++ 保持精准与克制，克服模型尽可能详细和完整的默认输出倾向
++++ 规则：简短回复，做任务前宣告，完成后一两句话总结
++++ 核心：区分问与做，针对探索性问题仅回建议，不直接动手
+++ 3. 工具使用指南
++++ 纠正模型的默认路径偏好，把能用变成用得好
++++ 规则：优先专用工具而非 Bash 命令（如 ReadFile 替代 cat，EditFile 替代 sed）
++++ 优化：引导多个独立调用在同轮内并行执行，省时省 Token
++++ 防错：使用绝对路径，且编辑文件前必须先读取
+++ 4. 代码质量规范
++++ 消除冗余：不添加超出需求的抽象、重构或未要求的未来设计
++++ 规范：默认不写 What 型注释，只在 Why 不明显时加一行短注释
+++ 5. 安全边界
++++ 作为 Prompt 层的软约束，与权限系统的硬约束形成互补防御
++++ 规则：不引入 OWASP 漏洞，不编造 URL，危险破坏性操作提前确认
+++ 6. 任务执行模式
++++ 引导模型自主感知和划分任务类型，匹配不同的执行策略
++++Bug 修复以最小修改和验证为主，新功能杜绝过度设计，模糊时先问
+++ 7. 输出风格
++++ 标准化代码引用格式（路径加行号），不使用情绪化表情符号，确保专业感

++ 二、 工具描述的 Prompt 强化
+++ 工具描述本质上是局部指令，而非单纯的代码注释
+++ 双重强化原则：关键规则（如专用工具优先、路径格式要求）在 System Prompt 和工具描述中同时重复
+++ 作用：模型在选择具体工具的决策点上被精准提醒，合规率显著提升

-- 三、 Prompt 组装管线与多源分发
--- 七个信息来源
++++ 静态 System Prompt、环境上下文、工具描述、项目指令 (MINIONCODE.md)、自动记忆、System Reminder、对话历史
--- 字段路由分发决策
++++ system 字段：存放静态 System Prompt 与环境上下文。内容保持稳定，最大化命中 Prompt Cache，降低 90% 的输入成本
++++ tools 字段：存放原子工具的 JSON Schema 描述
++++ messages 字段：存放对话历史、项目指令、记忆与 System Reminder。防止内容过长触发 Lost in the Middle 现象稀释注意力，且利于后续上下文压缩

-- 四、 动态指令注入机制 (System Reminder)
--- 核心定义
++++ 位于 messages 字段中、用 XML 标签包裹的系统补充指令
++++ 解决在会话进行中突发新状态（如连上新 MCP Server）需要立刻让模型感知的问题
--- 核心优势
++++ 注入 messages 字段完全不改动 system 字段，保护全局 Prompt Cache 不失效
++++ 避免了因修改 system prompt 导致后续每一轮都产生全价 input 计费的成本灾难
--- 典型场景
++++ MCP 增删、Skill 变更、子 Agent 类型声明、任务追踪进度等温和提醒

-- 五、 Prompt 成本控制矩阵
--- System 稳定性
++++ 维持 system 字段完全不变，提升缓存命中率，削减基础输入开销
--- Output 长度控制
++++ 通过行为准则压制模型废话，直接降低昂贵的输出 Token 计费
--- 工具调用高效率
++++ 并行化工具调用，大幅缩减 API 交互的总轮次

@endmindmap